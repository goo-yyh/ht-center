# 核心 API 的服务端地址，建议包含 /api/demo/v1 前缀
CORE_API_URL=http://127.0.0.1:3000/api/demo/v1

# 仅服务端使用，必须与 haitian 核心 API 配置一致
DEMO_SERVICE_TOKEN=haitian-demo-service-local
DEMO_SESSION_SECRET=haitian-demo-session-secret-local-2026

# HTTPS 部署时设置为 true；本地 HTTP 开发保持 false
DEMO_COOKIE_SECURE=false
